name: income_and_expense_tracker
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Your existing Firebase packages:
  firebase_auth: ^5.0.0
  google_sign_in: ^6.2.1
  # Add the missing bloc package:
  flutter_bloc: ^8.1.5
  fluttertoast: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
